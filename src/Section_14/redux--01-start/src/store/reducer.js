// we create the reducers in a separate file from the index.js file
// as we will be creating more complex reducers in our application 
const initialState = {
    counter: 0
}

const reducer = (state = initialState, action) => {
    return state;
};

export default reducer; 